# ⚡ 自动补全指南

## 概述

LLMSider 将 GitHub Copilot 风格的自动补全功能带到 Obsidian,在您输入时提供实时 AI 建议。无需离开编辑器即可获得智能代码和文本补全。

---

## 🎯 功能特性

### ✨ 内联建议
- **实时补全** - 输入时即时响应
- **多个建议** - 可循环查看选项
- **上下文感知** - 理解您的笔记库和写作风格
- **多语言支持** - 适用于代码块和普通文本

### 🚀 智能触发
- **自动触发** - 输入暂停后触发
- **手动触发** - 通过快捷键按需触发
- **可配置延迟** - 调整触发时间
- **粒度控制** - 行补全或完整补全

---

## 🔧 设置

### 启用自动补全

1. **打开设置**
   - Obsidian 设置 → LLMSider
   
2. **导航到自动补全部分**
   - 滚动到"自动补全设置"

3. **启用自动补全**
   - 切换"启用自动补全"开关
   - 选择您偏好的模型
   - 配置设置

### 配置选项

| 设置 | 描述 | 默认值 |
|---------|-------------|---------|
| **启用** | 开启/关闭自动补全 | 关闭 |
| **模型** | 用于建议的 AI 模型 | GPT-3.5 |
| **触发延迟** | 触发前等待时间(毫秒) | 500 |
| **粒度** | `line` 或 `full` 补全 | line |
| **最大令牌数** | 建议的最大长度 | 100 |
| **温度** | 创造力(0-1) | 0.3 |

### 推荐设置

**用于代码:**
```yaml
模型: GPT-4 / Claude 3.5 Sonnet
触发延迟: 300毫秒
粒度: line
温度: 0.2
```

**用于写作:**
```yaml
模型: GPT-3.5
触发延迟: 500毫秒
粒度: full
温度: 0.7
```

---

## 💡 使用自动补全

### 基本用法

1. **开始输入**
   ```markdown
   在生产力应用中使用 AI 的主要好处包括
   ```

2. **等待建议**
   ```markdown
   在生产力应用中使用 AI 的主要好处包括_
   ```
   ↑ 灰色幽灵文本出现

3. **接受或拒绝**
   - 按 `Tab` 接受
   - 按 `Esc` 拒绝
   - 继续输入以忽略

### 多个建议

当有多个选项可用时:

```markdown
今天的天气是 |
              └→ 建议 1/3
```

**导航:**
- `⌥ ]` (Option+]) - 下一个建议
- `⌥ [` (Option+[) - 上一个建议
- `Tab` - 接受当前建议
- `Esc` - 关闭所有建议

### 粒度模式

#### 行模式
仅补全当前行:

```markdown
输入:  def calculate_
输出: def calculate_total(items: list) -> float:
```

#### 完整模式
多行补全:

```markdown
输入:  def calculate_total(items: list):
输出: def calculate_total(items: list):
            """计算项目的总价格。"""
            total = sum(item.price for item in items)
            return round(total, 2)
```

---

## 🎨 视觉指示器

### 建议状态

**加载中:**
```markdown
正在输入...⏳
```

**建议可用:**
```markdown
您的文本 | 灰色 AI 建议
```

**多个选项:**
```markdown
您的文本 | 选项 1/3 ⟳
```

### 状态图标

| 图标 | 含义 |
|------|---------|
| ⏳ | 加载中 |
| ⟳ | 多个选项 |
| ✓ | 已接受 |
| ✗ | 已拒绝 |

---

## ⚙️ 高级配置

### 自定义触发条件

**最小字符数:**
```javascript
minChars: 2  // 仅在输入2+个字符后触发
```

**触发模式:**
```javascript
// 仅针对特定上下文触发
triggerPatterns: [
  /^def /,        // Python 函数
  /^function /,   // JavaScript 函数
  /^## /,         // Markdown 标题
]
```

### 上下文增强

**包含笔记库上下文:**
```yaml
useVaultContext: true  # 包含相关笔记
contextLimit: 3        # 最多包含的笔记数
```

**包含最近文件:**
```yaml
includeRecentFiles: true
recentFileCount: 5
```

### 性能调优

**防抖设置:**
```yaml
debounceMs: 500     # 触发前等待
minTypingPause: 200 # 触发的最小暂停时间
```

**缓存设置:**
```yaml
cacheEnabled: true
cacheTTL: 300000    # 5 分钟
maxCacheSize: 100   # 最大缓存建议数
```

---

## 💡 最佳实践

### 🎯 何时使用

**适合:**
- ✅ 补全常用短语
- ✅ 编写代码片段
- ✅ 填充样板内容
- ✅ 保持一致的格式
- ✅ 列表延续

**不太理想:**
- ❌ 创意写作初稿
- ❌ 高度专业化的领域内容
- ❌ 实时头脑风暴
- ❌ 敏感/私密信息

### 🚀 生产力技巧

1. **接受部分补全**
   ```markdown
   建议: "生产力应用包括任务管理器、笔记工具和日历应用"
   接受到: "生产力应用包括任务管理器"
   ```
   - 接受您想要的部分,然后继续输入

2. **用于样板内容**
   ```markdown
   输入: "---\ntitle: "
   接受: 完整的前言模板
   ```

3. **了解您的模型风格**
   - GPT-3.5: 快速、简洁
   - GPT-4: 详细、准确
   - Claude: 自然语言

4. **与快速聊天结合**
   - 使用自动补全进行快速补全
   - 使用快速聊天(Cmd+/)进行复杂编辑

### 🔒 隐私考虑

**会发送的内容:**
- 当前行或段落
- 文件上下文(如果启用)
- 笔记库元数据(如果启用)

**不会发送的内容:**
- 整个笔记库
- 其他打开的笔记
- 系统信息

**提示:**
- 对敏感数据使用本地模型(Ollama)
- 对私密笔记禁用笔记库上下文
- 定期检查设置

---

## 🐛 故障排除

### 常见问题

**建议不出现:**
1. ✅ 验证自动补全已启用
2. ✅ 检查模型是否激活
3. ✅ 确保 API 密钥有效
4. ✅ 等待触发延迟
5. ✅ 输入最少字符数(2+)

**建议速度慢:**
1. ✅ 使用更快的模型(GPT-3.5)
2. ✅ 减少最大令牌数
3. ✅ 禁用笔记库上下文
4. ✅ 增加防抖延迟
5. ✅ 检查网络速度

**无关的建议:**
1. ✅ 降低温度
2. ✅ 调整粒度
3. ✅ 提供更多上下文
4. ✅ 尝试不同的模型
5. ✅ 启用笔记库上下文

**建议闪烁:**
1. ✅ 增加触发延迟
2. ✅ 增加防抖时间
3. ✅ 禁用快速输入检测
4. ✅ 重启 Obsidian

### 调试模式

启用详细日志记录:

1. 设置 → LLMSider → 高级
2. 启用"调试日志"
3. 打开控制台 (Cmd+Option+I)
4. 查找自动补全日志:
   ```
   [LLMSider] 在第 42 行触发自动补全
   [LLMSider] 从 GPT-3.5 获取建议...
   [LLMSider] 已收到建议: "..."
   ```

---

## 🔑 键盘快捷键

| 操作 | 快捷键 | 描述 |
|--------|----------|-------------|
| 接受 | `Tab` | 接受当前建议 |
| 拒绝 | `Esc` | 关闭所有建议 |
| 下一个 | `⌥]` | 下一个建议选项 |
| 上一个 | `⌥[` | 上一个建议选项 |
| 触发 | `Ctrl+Space` | 手动触发(可选) |

### 自定义快捷键

设置位置: 设置 → 快捷键 → LLMSider

```yaml
接受建议: Tab
拒绝建议: Escape
循环下一个: Option+]
循环上一个: Option+[
手动触发: Ctrl+Space
```

---

## 📊 性能

### 模型比较

| 模型 | 速度 | 质量 | 成本 | 最适合 |
|-------|-------|---------|------|----------|
| **GPT-3.5** | ⚡⚡⚡ | ⭐⭐⭐ | 💰 | 常规使用 |
| **GPT-4** | ⚡ | ⭐⭐⭐⭐⭐ | 💰💰💰 | 代码、准确性 |
| **Claude 3** | ⚡⚡ | ⭐⭐⭐⭐ | 💰💰 | 自然语言 |
| **Ollama** | ⚡⚡⚡ | ⭐⭐⭐ | 免费 | 隐私、本地 |

### 优化技巧

**追求速度:**
1. 使用 GPT-3.5 或本地模型
2. 减少最大令牌数 (50-100)
3. 禁用笔记库上下文
4. 增加触发延迟

**追求质量:**
1. 使用 GPT-4 或 Claude 3
2. 增加最大令牌数 (200-500)
3. 启用笔记库上下文
4. 降低温度 (0.2-0.4)

**节省成本:**
1. 使用 GPT-3.5 或 Ollama
2. 设置合理的最大令牌数
3. 增加触发延迟
4. 缓存建议

---

## 🔄 与其他功能的集成

### 快速聊天
- 自动补全: 快速、内联
- 快速聊天: 复杂、多行
- **一起使用:** 自动补全提速,快速聊天求精准

### 选择弹出
- 自动补全: 生成新内容
- 选择: 修改现有内容
- **一起使用:** 自动补全来写,选择来优化

### 聊天视图
- 自动补全: 静默、后台
- 聊天: 交互式、对话式
- **一起使用:** 自动补全保持流畅,聊天处理复杂任务

---

## 📚 示例

### 代码补全

**Python:**
```python
输入:  def process_data(
输出: def process_data(data: list) -> dict:
            """处理输入数据并返回结果。"""
            # 实现代码
            return {}
```

**JavaScript:**
```javascript
输入:  const fetchUser
输出: const fetchUser = async (userId) => {
            const response = await fetch(`/api/users/${userId}`);
            return response.json();
        };
```

### 文本补全

**博客文章:**
```markdown
输入:  AI 在生产力方面的未来
输出: AI 在生产力工具方面的未来看起来非常有前景,
        自然语言处理的进步使界面更加直观,
        自动化更加智能。
```

**电子邮件:**
```markdown
输入:  感谢您的
输出: 感谢您的电子邮件。我会审查这个提案并在
        本周末之前回复您。
```

---

## 📖 相关指南

- [快速聊天](quick-chat.md) - 内联 AI 辅助
- [选择弹出](selection-popup.md) - 右键菜单操作
- [聊天界面](chat-interface.md) - 完整对话模式
- [设置指南](settings-guide.md) - 完整设置

---

**需要帮助?** [GitHub Issues](https://github.com/llmsider/obsidian-llmsider/issues) | [Discord](https://discord.gg/llmsider)
